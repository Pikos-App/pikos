.DS_Store
node_modules
coverage
package-lock.json
build
apps/desktop/dist
packages/pikos-bridge/dist
.svelte-kit
package
.turbo
.env
.env.*
!.env.example
vite.config.js.timestamp-*
vite.config.ts.timestamp-*

# Rust build artifacts
src-tauri/target/
/target
**/*.rs.bk
*.pdb

# IDE files
.vscode/
.idea/
*.swp
*.swo

# Playwright
playwright-report/
blob-report/
all-blob-reports/
test-results/
recordings/

# Calendar layout screenshots (regen via pnpm --filter @pikos/desktop screenshot:calendar)
.screenshots/

# Ad-hoc screenshots land at whatever cwd a Playwright run used, which is a
# package root. No real asset lives there — icons, favicons and brand art are
# all in nested directories — so ignoring that one level can't hide one.
/*.png
/apps/*/*.png

# Incremental typecheck state (tsc -b)
*.tsbuildinfo

# Claude Code
.claude/

# Agent workspace (private — not for public repo)
.agent/
CLAUDE.md

# OS generated files
Thumbs.db

# iOS build artifacts.
# The XCFramework is a multi-megabyte binary produced by
# scripts/build-ios-framework.sh on a Mac. The Swift bindings beside it ARE
# committed — those are reviewable source, generated on any platform.
apps/ios/**/Frameworks/
apps/ios/**/.build/
apps/ios/**/*.xcframework

# Built mobile editor bundle (scripts/build-editor-bundle.sh).
# A generated single-file build, not source.
apps/ios/PikosEditorBridge/Sources/PikosEditorBridge/Resources/editor.html
packages/editor-mobile/dist/

# Playwright output for the mobile editor suite.
packages/editor-mobile/test-results/
packages/editor-mobile/playwright-report/

# ts-rs writes here when TS_RS_EXPORT_DIR is unset (a bare `cargo test`); the
# committed copy lives in packages/core/src/generated via scripts/gen-ts-bindings.sh.
crates/pikos-db/bindings/
