# Feature: Tags — Future Work

Tag picker in metadata header is shipped. Tag storage as JSON array in `pages.tags` is working.

## Unbuilt Features

- **Tags panel in sidebar**: Collapsible section showing all tags with page counts. Click to open tag view.
- **Tag view**: Replaces pages list for selected tag (cross-folder view).
- **Tag filter in page list (GOO-38)**: Multi-select tag filter in filter bar.
- **`#tag` in editor body (Phase 2)**: Typing `#tag` in content syncs to `tags` column on save.
- **Calendar filter by tag**: Filter scheduled pages by tag.
